-- Disabled deliberately, and it changes nothing.
--
-- This script sits in a FOLDER inside StarterCharacterScripts, so when it is copied
-- into the character its script.Parent is that folder, not the character. Line 32 then
-- waits for a HumanoidRootPart that will never appear there, and the script hangs on
-- that line forever -- printing an "Infinite yield possible" warning on every single
-- spawn and never executing a further line. It has never run.
--
-- So disabling it removes the warning and nothing else. The alternative -- moving it up
-- a level so it finally works -- would switch on an immersive camera that takes over
-- from StarterPlayerScripts.ShiftLock, which is a behaviour change nobody has ever seen
-- and not one to make silently. The original source is in the place's history if it is
-- ever wanted.
